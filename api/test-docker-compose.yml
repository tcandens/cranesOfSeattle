testdb:
  image: mdillon/postgis:latest
  environment:
    - POSTGRES_DB=test

testapi:
  build: .
  dockerfile: Dockerfile.test
  environment:
    - ENV=TEST
    - PGDATABASE=test
    - PGUSER=postgres
    - PGPASSWORD=mysecretpassword
  volumes:
    - ./:/var/www/api
  links:
    - testdb:db
